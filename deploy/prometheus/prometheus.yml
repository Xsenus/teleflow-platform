global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    service: teleflow-platform

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: teleflow-api
    metrics_path: /metrics
    static_configs:
      - targets: ["api:8080"]
